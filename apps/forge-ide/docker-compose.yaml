version: "3.7"
x-project-base:
  &project-base
  restart: always
  networks:
    - remixide

networks:
  remixide:

services:
  forgeide:
    <<: *project-base
    image: creditchain/forge-ide:$TAG
    container_name: forgeide-${TAG}
    ports: 
      - 8080:80
      - 65520:65520
