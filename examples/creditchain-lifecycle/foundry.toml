[profile.default]
src = "src"
out = "out"
test = "test"
libs = []
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
evm_version = "paris"

[rpc_endpoints]
creditchain_mainnet = "${CREDITCHAIN_MAINNET_RPC_URL}"
creditchain_testnet = "${CREDITCHAIN_TESTNET_RPC_URL}"
creditchain_devnet = "${CREDITCHAIN_DEVNET_RPC_URL}"
